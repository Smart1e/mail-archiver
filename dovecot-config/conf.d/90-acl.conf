# Enforce read-only at the ACL layer. `lookup read write-seen` lets clients list the mailbox,
# fetch messages, and persist the \Seen flag in Dovecot's own per-user index. No insert/expunge
# — the archive is the source of truth.

plugin {
  acl = vfile:/etc/dovecot/dovecot-acl:cache_secs=300
}
