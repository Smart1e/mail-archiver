version: '3.8'

networks:
  postgres:

services:
  mailarchive-app:
    build: .
    restart: always
    ports:
      - "5001:5000"
      - "143:143"
      - "993:993"
      - "465:465"
      - "25:25"
    environment:
      - ImapServer__Enabled=true
      - ImapServer__Port=143
      - ImapServer__ImapsPort=993
      - ImapServer__SmtpPort=25
      - ImapServer__SmtpsPort=465
      - ImapServer__CaCertExportPath=/app/certs/mailarchiver-ca.cer
    networks:
      - postgres
    volumes:
      - ./appsettings.json:/app/appsettings.json
      - ./logs:/app/logs
      - ./data-protection-keys:/app/DataProtection-Keys
      - ./certs:/app/certs
    depends_on:
      - postgres

  postgres:
    image: postgres:14-alpine
    restart: always
    environment:
      POSTGRES_DB: MailArchiver
      POSTGRES_USER: mailuser
      POSTGRES_PASSWORD: masterkey
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    networks:
      - postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mailuser -d MailArchiver"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
